const letras = ['a', 'b', 'c', 'd'];


//final, elimina y devuelve elemento final
// const final = letras.pop();

//elimina devuelve el primer elemtno de un array
// const comienzo = letras.shift()

//eliminar de el medio (indice donde empieza,cantidad que elminia(si le doy 0 no elminar nada))
letras.splice(1, 2);

console.log(letras);
