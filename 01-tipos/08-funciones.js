/* listado de instrucciones que deben hace
lo que escriba a lado de function sera el nombre de la funcion */

function saludar() {
    console.log('Hola Mundo');
}

/*si no llamo a la funcion no va a pasar nada, por eso
aca abajo llamo a la funcion saludar */
saludar();


function suma() {
    return 2 + 2;
}

let resultado = suma();
console.log(resultado);


